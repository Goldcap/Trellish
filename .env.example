# Server
PORT=3001
APP_PASSWORD=your_shared_passphrase
DB_PATH=/app/server/data/dasboot.db

# Twilio (SMS) — leave blank to disable
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Nodemailer (Email) — leave blank to disable
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
